Generate ES|QL query suggestions from these enriched discoveries. You MUST produce at least one suggestion per discovery.

For each discovery:
1. Read the discovery title, description, severity, and stream_refs
2. Generate 1-3 ES|QL queries that would be useful for monitoring, alerting, or visualizing the pattern described
3. Use the stream names from stream_refs in your FROM clauses
4. Include the discovery UUID in discovery_refs

`enriched discoveries`
{{{discoveries}}}
